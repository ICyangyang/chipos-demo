{
  "file_path": "/workspace/icwork/uart_tx/01_rtl/Uart_tx.v",
  "old_string": "TX_BIT0: begin\n                if (bit_cnt == 4'd15 && enable) begin\n                    if (data_bits_enc == 2'b00 && bit_index == 3'd0) // 5-bit: only BIT0..BIT4\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else\n                        next_state = TX_BIT1;\n                end else\n                    next_state = TX_BIT0;\n            end\n\n            TX_BIT1: begin\n                if (bit_cnt == 4'd15 && enable) begin\n                    if (data_bits_enc == 2'b00 && bit_index == 3'd1) // 5-bit done\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else if (data_bits_enc == 2'b01 && bit_index == 3'd1) // 6-bit: only BIT0..BIT5\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else\n                        next_state = TX_BIT2;\n                end else\n                    next_state = TX_BIT1;\n            end\n\n            TX_BIT2: begin\n                if (bit_cnt == 4'd15 && enable) begin\n                    if (data_bits_enc == 2'b01 && bit_index == 3'd2) // 6-bit done\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else if (data_bits_enc == 2'b10 && bit_index == 3'd2) // 7-bit: only BIT0..BIT6\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else\n                        next_state = TX_BIT3;\n                end else\n                    next_state = TX_BIT2;\n            end\n\n            TX_BIT3: begin\n                if (bit_cnt == 4'd15 && enable) begin\n                    if (data_bits_enc == 2'b10 && bit_index == 3'd3) // 7-bit done\n                        next_state = parity_en ? TX_PARITY : TX_STOP1;\n                    else\n                        next_state = TX_BIT4;\n                end else\n                    next_state = TX_BIT3;\n            end",
  "new_string": "TX_BIT0: begin\n                if (bit_cnt == 4'd15 && enable)\n                    next_state = TX_BIT1;\n                else\n                    next_state = TX_BIT0;\n            end\n\n            TX_BIT1: begin\n                if (bit_cnt == 4'd15 && enable)\n                    next_state = TX_BIT2;\n                else\n                    next_state = TX_BIT1;\n            end\n\n            TX_BIT2: begin\n                if (bit_cnt == 4'd15 && enable)\n                    next_state = TX_BIT3;\n                else\n                    next_state = TX_BIT2;\n            end\n\n            TX_BIT3: begin\n                if (bit_cnt == 4'd15 && enable)\n                    next_state = TX_BIT4;\n                else\n                    next_state = TX_BIT3;\n            end"
}